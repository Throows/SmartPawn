name: Build-MacOS

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on:  macos-latest
    steps:
      - uses:  actions/checkout@v3

      - name: Install sfml
        run: |
          brew update
          brew install python@3.10 pybind11 sfml
          brew upgrade

      - name: Install xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: latest
          actions-cache-folder: '.xmake-cache'

      - name: Build SmartPawn
        run: xmake --yes --verbose
      
      - uses: actions/upload-artifact@v3
        with:
          name: SmartPawn-macOS-arm64
          path: bin/macos-arm64
